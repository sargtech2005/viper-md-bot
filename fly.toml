# fly.toml — Viper Bot Fly.io config (Lightning ⚡)

app = "viper-md-bot"
primary_region = "ams"

[build]

[deploy]
  # "immediate" is correct for single-machine apps.
  # "rolling" requires 2+ machines — on 1 machine it deadlocks:
  # Fly keeps the old machine alive waiting to replace it, but there's
  # nowhere to put the new one, so both fail with "machine still active".
  strategy = "immediate"

[env]
  NODE_ENV = "production"
  NODE_OPTIONS = "--max-old-space-size=3072"  # 3GB of 4GB RAM for Node heap

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false   # NEVER sleep — keep both machines always hot
  auto_start_machines = true
  min_machines_running = 2     # 2 machines — both must always be running

  [http_service.concurrency]
    type = "connections"
    hard_limit = 1000
    soft_limit = 800

[[vm]]
  memory = '4gb'
  cpu_kind = 'shared'
  cpus = 2

[checks]
  [checks.health]
    grace_period = "40s"
    interval = "15s"
    method = "GET"
    path = "/health"
    port = 3000
    timeout = "20s"
    type = "http"
